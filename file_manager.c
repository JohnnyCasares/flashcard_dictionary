#include <stdio.h>
int dictionaryLookUp(char japanese_word[]) {
  // file dictionary
  // open to read to search for word
  // if word is found then display meaning
  // else ask user to add the word
  printf("%s", japanese_word);
  return 0;
}